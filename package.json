{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/migrate.js",
    "docker:build": "docker build --platform linux/amd64 --provenance=false --sbom=false -t lovely-portfolio:local ."
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.1106.0",
    "compression": "^1.7.4",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.2",
    "helmet": "^7.1.0",
    "multer": "^2.2.0",
    "pg": "^8.12.0"
  }
}
